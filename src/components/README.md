# Components

Hier kommen die Extension-spezifischen Komponenten.

Bewährte Patterns:
- Selektion: api.viewer.setSelection([123,456]) — plain Array!
- Properties: max 10 IDs pro getObjectProperties() Aufruf
- PSet-Matching: immer case-insensitive (includes)
- Modelle: modelId verwenden, nicht id
